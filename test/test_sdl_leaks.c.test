#include "SDL2/SDL.h"

int main(void)
{
	SDL_Init(SDL_INIT_VIDEO);
	SDL_Window *w = SDL_CreateWindow("wolf3d_client", SDL_WINDOWPOS_UNDEFINED,
		SDL_WINDOWPOS_UNDEFINED, 400, 400, 0);
	SDL_DestroyWindow(w);
	SDL_Quit();
	return (0);
}
